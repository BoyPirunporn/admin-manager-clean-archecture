spring:
  datasource:
    url: password # Make sure this is your DB name
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        '[format_sql]': true
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Bangkok
  
  mail:
    host: smtp.gmail.com
    port: 587
    username: username
    password: password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
      
logging:
  pattern:
    console: "[%d{yyyy-MM-dd HH:mm:ss}] [ %-5level ] [ %logger{20} ] - %msg%n"
  level:
    root: INFO
    org:
      thymeleaf: INFO
    
application:
    initial-data:
      default:
        super-role-name: SUPER_ADMIN
        user: 
          email: lufy@admin.com
          password: P@ssw0rd
          firstName: Monkey D
          lastName: Lufy

    mail:
      verify: 
        expiry: 604800 # sec
      email-template-path: template-email
    frontend:
      url: url
    api:
        version: /api/v1
    security:
      jwt:
        secret-key: secretkey
        expiration: 900000
        refresh-token:
          expiration: 3600000

